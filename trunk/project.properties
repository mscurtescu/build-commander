component.name=build-commander

debug=true
jvm.ver=1.5

src=${basedir}/src

test.src=${basedir}/test/src
test.cfg=${basedir}/test/config

lib.dir=${basedir}/lib
libext.dir=${basedir}/libext

dist=${basedir}/dist
build=${basedir}/build
classes=${build}/classes
classes.test.junit=${build}/classes-test/junit
apidoc=${build}/apidoc
